{% extends "base.html.twig" %}


{% block title
 %}{% endblock %}


{% block body %}

	<h1>Modifier
		{{category.name}}</h1>


	{{form_start(formView)}}
	<div class="row">
		<div class="col">
			{{form_row(formView.name)}}
		</div>
	</div>

	<button type="submit" class="btn btn-primary">
		<i class="fa fas-save"></i>
		Enregistrer
	</button>

	{{form_end(formView)}}

{% endblock %}
